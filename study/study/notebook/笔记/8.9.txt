int arr[常量] 		//可define SIZE 12 	定义常量


字符串：
	1.放在只读取：


数组（一维）：
	1.什么时候确认大小：编译时候
	2.什么时候分配内存：运行
	3.最大分配内存：栈（1M或2M）
	4.数组名为数组首元素的地址

	/*
	指针+数字-->指针的类型（int、char）
	arr[i] <==> *(arr + i)	     []自带引用
	*/
	
	数组（低地址------------>高地址）
	

	栈（低地址<------------高地址）（先进后出）
	栈每个元素中8个字节中有2个（哨兵位），防止数组下标越界
	只有在程序运行完之后才崩溃

数组（二维）：
	1.行可省略，列不可省略
	2.数组名为数组首元素（一维数组）的地址   int（*p）[元素]
	数组指针：/*	int arr[3] =  {1,2,3}
		int (*p)[3] = &arr	*/